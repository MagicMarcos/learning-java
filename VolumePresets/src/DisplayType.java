/**
 * Type-safe enum for the various display types of Television object
 * There are NOT strings
 * These are DisplayType objs and there are only 5 (fixed num) instances of DisplayType
 */

enum DisplayType {
    LED, OLED, PLASMA, LCD, CRT
}